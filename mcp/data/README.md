# Runtime catalog copy

`manifest.json` is the read-only public catalog snapshot used by the MCP tools. It contains metadata only and does not include firmware binaries.

The canonical public source is [`../../data/manifest.json`](../../data/manifest.json). Keep this runtime copy byte-identical to that source when the catalog is updated.

